{% for asset in assets %}
    {% if asset.inline %}
        <script hx-preserve="true" id="{{ asset.id }}">
            {{ asset.source }}
        </script>
    {% else %}
        <script hx-preserve="true" id="{{ asset.id }}" src="{{ asset.source }}"></script>
    {% endif %}
{% endfor %}

{% for asset in assets %}
    {% if asset.inline %}
        <style hx-preserve="true" id="{{ asset.id }}">
            {{ asset.source }}
        </style>
    {% else %}
        <link hx-preserve="true" id="{{ asset.id }}" rel="stylesheet" href="{{ asset.source }}" />
    {% endif %}
{% endfor %}
